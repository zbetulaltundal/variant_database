# @author: Zeynep Betül Altundal

import clingen_import as clingen
import civic_import as civic
import clinvar_import as clinvar
import pharmgkb_import as pharmgkb
import db_config
import common_functions


if __name__ == "__main__":
    
    # conn = common_functions.db_connect(db_config.CIVIC_DB_NAME)

    # civic.import_civic_data(conn)
    
    # conn = db_connect(db_config.CLINGEN_DB_NAME)

    # clingen.import_clingen_data(conn)
    
   #  conn = common_functions.db_connect(db_config.CLINVAR_DB_NAME)

   #  clinvar.import_clinvar_data(conn)
    
   #  if conn:
   #     conn.close()

    # conn = common_functions.db_connect(db_config.PHARMGKB_DB_NAME)
    # pharmgkb.import_pharmgkb(conn)
    # if conn:
    #     conn.close()
